const { Router } = require('express');

const router = Router();

// Função de criar
router.post('/', )

// Função de editar
router.put('/:id', ) // Paramentro id

// Função de deletar
router.delete('/:id', ) // Paramentro id

// Função buscar unico
router.get('/:id', ) // Paramentro id

// Função buscar todos
router.get('/', )


module.exports = router;